# Pravna informatika - NLP servis
# VLASNIK: Član 2

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# NLP - osnovni
#classla #je glavni za srpski jezik
#classla==2.1.0       # otkomentariši kad se inicijalizuje pravi NLP
spacy
# Opciono (skidanje preko HF transformers za BERTić, XLM-R)
# transformers==4.40.0
# torch==2.3.0
# sentencepiece==0.2.0

# LLM (lokalno, besplatno) — Celina 4 (ekstrakcija iz teksta) i Celina 9 (generisanje odluke)
# Koristi se Ollama preko HTTP-a (httpx, ispod). Nije potreban API ključ.
#   1) instaliraj Ollama: https://ollama.com
#   2) povuci model:  ollama pull mistral   (ili llama3.2:3b / qwen2.5:3b za slabiji hardver)
#   3) opciono podesi: OLLAMA_MODEL, OLLAMA_URL
# httpx se koristi i u runtime-u (ne samo za testove).

# Testiranje
pytest==8.2.0
httpx==0.27.0
